一、设计目标与原则
1.1 核心诉求
设计一套 结构化的卡片描述语言（Card DSL），满足以下需求：
1. AI 可直接生成 — LLM 输出纯文本即可创建功能完备的卡片，无需 JSON/HTML
2. 人类可读可编辑 — 类 Markdown 语法，用户手动编写也很直观
3. App 端易解析 — Swift/JS 均可用正则 + 递归下降轻松解析
4. 功能完备 — 支持单选、多选、填空、答案线、卡片引用、富文本样式等
5. 可扩展 — 新增题型无需大改语法
1.2 设计原则
原则
说明
约定优于配置
尽量少的元数据，通过语法结构自动推断题型
Markdown 兼容
基础富文本（加粗、斜体、代码）复用 Markdown 语法
块级分隔明确
用 --- 分隔卡片正反面，用 === 分隔不同卡片
标记语义化
每种交互元素都有专属标记，不产生歧义

---
二、整体格式结构
2.1 单张卡片结构
@card
#type: basic | cloze | choice | multi_choice | fill | composite
#tags: 标签1, 标签2
#deck: 牌组名称
#difficulty: easy | medium | hard
---front---
（正面内容：题目/问题）
---back---
（背面内容：答案/解析）
---meta---
（可选：解析、来源、关联卡片等）
@end
2.2 多张卡片的文件结构
@card
...第一张卡片...
@end

===

@card
...第二张卡片...
@end

===

@card
...第三张卡片...
@end
=== 作为卡片间分隔符，清晰直观。

---
三、富文本标记语法
3.1 基础样式（兼容 Markdown）
语法
渲染效果
示例
**文字**
加粗
**重点概念**
*文字*
斜体
*注意区分*
`文字`
行内代码
`O(n log n)`
~~文字~~
删除线
~~错误答案~~
==文字==
高亮标记
==核心考点==
> 文字
引用块
> 出自《算法导论》
3.2 扩展样式（Card DSL 专属）
语法
渲染效果
说明
{red:文字}
红色文字
颜色标记，支持 red/blue/green/orange/purple/gray
{bg:yellow:文字}
黄色背景高亮
背景色标记
{size:lg:文字}
大号文字
支持 sm/md/lg/xl
{u:文字}
下划线
下划线标记
{sup:文字}
上标
如 H{sup:2}O
{sub:文字}
下标
如 CO{sub:2}
{kbd:文字}
键盘按键样式
如 {kbd:Ctrl} + {kbd:C}
3.3 样式嵌套
样式可以互相嵌套使用：
这是 **{red:加粗红色}** 的文字
这是 {bg:yellow:**高亮加粗**} 的文字
公式：H{sub:2}O 的摩尔质量是 {blue:18 g/mol}

---
四、交互元素语法
4.1 填空题 {{...}}
双花括号标记填空位，AI 和用户都容易理解。
基础填空：
TCP 建立连接需要 {{三次握手}}，断开连接需要 {{四次挥手}}。
带提示的填空：
HTTP 默认端口是 {{80|提示:常用端口号}}，HTTPS 是 {{443|提示:加密端口}}。
带编号的填空（多个填空生成多张卡片，类似 Anki Cloze）：
{{c1:TCP}} 是面向连接的，{{c2:UDP}} 是无连接的。
c1 和 c2 分别生成独立卡片，复习时只遮挡对应编号。
正则匹配填空（允许多种正确答案）：
中国的首都是 {{北京|Beijing|Peking|regex:北京|Beijing}}。
4.2 单选题 (x)
用圆括号标记选项，(x) 表示正确答案：
以下哪个排序算法的平均时间复杂度是 O(n log n)？

( ) 冒泡排序
(x) 快速排序
( ) 插入排序
( ) 选择排序
渲染时显示为单选按钮 ○/●，用户点选后与正确答案对比。
4.3 多选题 [x]
用方括号标记选项，[x] 表示正确答案（可多个）：
以下哪些是 HTTP 请求方法？（多选）


[x] GET


[x] POST


[ ] CONNECT_DB


[x] PUT


[ ] SEND
渲染时显示为复选框 ☐/☑，需全部选对才算正确。
4.4 答案线 ___
三个或以上下划线表示答案线，用户在上面手写或输入答案：
请写出快速排序的时间复杂度：

最好情况：_______________
最坏情况：_______________
平均情况：_______________
带预期答案的答案线（翻面后显示对照）：
请写出快速排序的时间复杂度：

最好情况：___(O(n log n))___
最坏情况：___(O(n²))___
平均情况：___(O(n log n))___
括号内是期望答案，正面不显示，翻转后或提交后显示对照。
4.5 排序题 1>2>3
请将以下排序算法按平均时间复杂度从低到高排列：

{sort}
- [3] 冒泡排序 O(n²)
- [1] 归并排序 O(n log n)
- [2] 希尔排序 O(n^1.3)
- [4] 猴子排序 O(n·n!)
{/sort}
方括号中的数字是正确顺序，显示时打乱，用户拖拽排序。
4.6 匹配题 <->
请将左右两列对应连线：

{match}
TCP <-> 面向连接、可靠传输
UDP <-> 无连接、尽力交付
HTTP <-> 超文本传输协议
FTP <-> 文件传输协议
{/match}
渲染为左右两列，用户拖线连接。
4.7 判断题 {T} / {F}
{T} TCP 是面向连接的协议。
{F} UDP 提供可靠传输。
{T} HTTP/2 支持多路复用。
{T} = True（正确），{F} = False（错误）。

---
五、高级功能语法
5.1 卡片引用 @ref
引用其他卡片，实现知识图谱关联：
@card
#type: basic
#id: tcp-handshake
---front---
TCP 三次握手的过程是什么？
---back---
1. 客户端发送 SYN
2. 服务端回复 SYN+ACK
3. 客户端发送 ACK

相关卡片：@ref[tcp-wave]{TCP 四次挥手} | @ref[tcp-vs-udp]{TCP 与 UDP 对比}
---meta---
@source: 《计算机网络：自顶向下方法》第 3 章
@end
引用语法： @ref[卡片ID]{显示文字}
- 点击后跳转到被引用的卡片
- 构建知识关联网络
5.2 图片与媒体
!img[图片描述](url_or_local_path)
!audio[音频描述](url_or_local_path)
示例：
---front---
这个网络拓扑属于什么类型？

!img[网络拓扑图](assets/topology.png)

( ) 星型
(x) 环型
( ) 总线型
5.3 代码块
---front---
以下 Python 代码的输出是什么？

```python
a = [1, 2, 3]
b = a
b.append(4)
print(a)
```

---back---
输出：`[1, 2, 3, 4]`

**解析：** Python 中列表赋值是 {red:引用传递}，`a` 和 `b` 指向同一个对象。
5.4 条件显示
{hint:点击查看提示}
这道题可以用动态规划来解。
{/hint}

{explain:点击查看详细解析}
时间复杂度分析：
- 状态转移方程：dp[i] = max(dp[i-1], dp[i-2] + nums[i])
- 空间可优化为 O(1)
{/explain}
渲染为折叠区域，点击展开。hint 是提示（复习时可选看），explain 是解析（答题后显示）。
5.5 分组/分栏
{columns:2}
|||
**优点：**
- 速度快
- 内存占用小
|||
**缺点：**
- 不稳定
- 最坏 O(n²)
|||
{/columns}
5.6 数学公式（LaTeX）
求解以下积分：

$$\int_0^1 x^2 \, dx$$

答案：$\frac{1}{3}$
行内公式用 $...$，块级公式用 $$...$$。

---
六、复合卡片（Composite）
一张卡片中混合多种交互元素，这是 Card DSL 最强大的能力：
6.1 示例：综合题
@card
#type: composite
#tags: 计算机网络, TCP
#deck: 考研408
---front---
## TCP 协议综合题

**1. 填空：**
TCP 的全称是 {{Transmission Control Protocol|传输控制协议}}。

**2. 单选：**
TCP 报文段头部最小长度是多少字节？
( ) 10
(x) 20
( ) 30
( ) 40

**3. 多选：**
以下哪些是 TCP 的特性？

[x] 面向连接


[x] 可靠传输


[ ] 无序交付


[x] 流量控制


[ ] 不保证交付


**4. 简答：**
请简述 TCP 拥塞控制的四个阶段：
_______________

---back---
**1.** Transmission Control Protocol（传输控制协议）

**2.** B. 20 字节（固定头部 20 字节，选项最多扩展到 60 字节）

**3.** A/B/D
- {green:✓ 面向连接} — 需三次握手建立连接
- {green:✓ 可靠传输} — 确认重传机制
- {red:✗ 无序交付} — TCP 保证有序
- {green:✓ 流量控制} — 滑动窗口机制
- {red:✗ 不保证交付} — 这是 UDP 的特性

**4.** 参考答案：
1. ==慢开始==（Slow Start）
2. ==拥塞避免==（Congestion Avoidance）
3. ==快重传==（Fast Retransmit）
4. ==快恢复==（Fast Recovery）

{explain:详细解析}
拥塞窗口 cwnd 的变化过程：
- 慢开始：cwnd 指数增长（1→2→4→8...）
- 到达 ssthresh 后进入拥塞避免：cwnd 线性增长
- 收到 3 个重复 ACK → 快重传 + 快恢复
- 超时 → ssthresh 减半，cwnd 重置为 1，回到慢开始
{/explain}

---meta---
@source: 《计算机网络》谢希仁 第七版 第 5 章
@ref: tcp-handshake, tcp-wave
@end

---
七、AI 生成 Prompt 模板
7.1 给 AI 的系统指令
你是一个闪卡生成助手。用户提供学习材料后，你需要按照 Card DSL 格式生成记忆卡片。

## 格式规范

每张卡片结构如下：
- @card / @end 包裹
- #type 声明题型：basic / cloze / choice / multi_choice / fill / composite
- ---front--- 和 ---back--- 分隔正反面
- 可选 ---meta--- 附加元信息

## 交互元素语法
- 填空：{{答案}} 或 {{答案|提示:xxx}}
- 单选：(x) 正确选项 / ( ) 错误选项
- 多选：[x] 正确选项 / [ ] 错误选项
- 答案线：___(期望答案)___
- 判断：{T} 正确陈述 / {F} 错误陈述

## 富文本语法
- **加粗** *斜体* `代码` ==高亮==
- {red:红色文字} {blue:蓝色文字} {green:绿色文字}
- @ref[card-id]{显示文字} 引用其他卡片
- $LaTeX$ 行内公式 / $$LaTeX$$ 块级公式

## 生成要求
1. 优先生成填空题和选择题，便于客观评分
2. 每张卡片聚焦一个知识点
3. 正面的题目要有区分度，避免过于简单
4. 背面提供解析，用 {red:} {green:} 标注对错
5. 添加 @ref 关联相关知识点的卡片
6. 用 === 分隔多张卡片
7.2 用户调用示例
用户输入：
请根据以下内容生成闪卡：

快速排序是一种分治策略的排序算法。它选择一个基准元素，
将数组分为两部分：小于基准的和大于基准的，然后递归排序。
平均时间复杂度 O(n log n)，最坏 O(n²)（有序数组时）。
空间复杂度 O(log n)。它是不稳定排序。
AI 输出：
@card
#type: cloze
#tags: 排序算法, 快速排序
#deck: 数据结构与算法
---front---
快速排序采用 {{c1:分治}} 策略，选择一个 {{c2:基准元素}}，
将数组分为两部分，然后 {{c3:递归}} 排序各部分。
---back---
- 分治（Divide and Conquer）：将问题分解为子问题分别解决
- 基准元素（Pivot）：用于划分数组的参照值
- 递归：对左右子数组重复执行相同操作
@end

===

@card
#type: choice
#tags: 排序算法, 时间复杂度
#deck: 数据结构与算法
---front---
快速排序的 **平均** 时间复杂度是？

( ) O(n)
(x) O(n log n)
( ) O(n²)
( ) O(log n)
---back---
**B. O(n log n)**

| 情况 | 时间复杂度 | 触发条件 |
|---|---|---|
| 最好 | {green:O(n log n)} | 每次基准恰好中分 |
| 平均 | {green:O(n log n)} | 随机数据 |
| 最坏 | {red:O(n²)} | 数组已有序 |

@ref[merge-sort]{归并排序} 也是 O(n log n) 但是==稳定==的。
@end

===

@card
#type: multi_choice
#tags: 排序算法, 快速排序
#deck: 数据结构与算法
---front---
以下关于快速排序的描述，哪些是 **正确** 的？（多选）


[x] 平均时间复杂度 O(n log n)


[ ] 最坏时间复杂度 O(n log n)


[x] 空间复杂度 O(log n)


[ ] 是稳定排序算法


[x] 采用分治策略

---back---
- {green:✓ 平均时间复杂度 O(n log n)} — 正确
- {red:✗ 最坏时间复杂度 O(n log n)} — 最坏是 O(n²)，有序数组时退化
- {green:✓ 空间复杂度 O(log n)} — 递归栈的深度
- {red:✗ 是稳定排序} — 快速排序是 **不稳定** 的（相同元素可能交换顺序）
- {green:✓ 采用分治策略} — 分而治之，递归解决
@end

---
八、格式解析架构
8.1 解析流程
Card DSL 文本
     │
     ▼
┌─────────────┐
│   Tokenizer  │  按 === 分割为单张卡片文本
│   分词器      │  按 ---xxx--- 分割为 front/back/meta 区块
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Block Parser│  解析 #type, #tags 等元数据
│  块级解析器   │  识别交互元素块（choice, match, sort 等）
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Inline Parser│  解析行内标记：
│ 行内解析器    │  **bold**, {{cloze}}, {red:color},
│              │  @ref[], $LaTeX$ 等
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   AST Node   │  生成抽象语法树
│  语法树节点    │  每个节点对应一个 UI 组件
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  SwiftUI     │  遍历 AST 树，递归渲染为
│  Renderer    │  对应的 SwiftUI View 组件
└─────────────┘
8.2 AST 节点类型定义
/// 卡片 AST 节点
indirect enum CardNode {
    // 文档结构
    case card(meta: CardMeta, front: [CardNode], back: [CardNode], extra: [CardNode]?)

    // 块级元素
    case paragraph([CardNode])           // 段落
    case heading(level: Int, [CardNode]) // 标题
    case codeBlock(lang: String?, code: String)  // 代码块
    case blockquote([CardNode])          // 引用块
    case image(alt: String, url: String) // 图片
    case divider                         // 分隔线
    case mathBlock(latex: String)        // 块级公式
    case columns(count: Int, cols: [[CardNode]]) // 分栏

    // 交互元素（块级）
    case choiceGroup(options: [ChoiceOption], multiSelect: Bool)
    case answerLine(expected: String?)
    case sortGroup(items: [SortItem])
    case matchGroup(pairs: [MatchPair])
    case judgeItem(isTrue: Bool, text: [CardNode])
    case hintBlock(label: String, content: [CardNode])

    // 行内元素
    case text(String)                    // 纯文本
    case bold([CardNode])                // 加粗
    case italic([CardNode])              // 斜体
    case inlineCode(String)              // 行内代码
    case strikethrough([CardNode])       // 删除线
    case highlight([CardNode])           // 高亮 ==...==
    case underline([CardNode])           // 下划线
    case colored(color: String, [CardNode])      // 彩色文字
    case bgColored(color: String, [CardNode])    // 背景色
    case sized(size: String, [CardNode])         // 字号
    case superscript([CardNode])         // 上标
    case subscriptNode([CardNode])       // 下标
    case kbd(String)                     // 键盘按键
    case cloze(id: Int?, answer: String, hint: String?) // 填空
    case cardRef(id: String, label: String)      // 卡片引用
    case mathInline(latex: String)       // 行内公式
}

struct CardMeta {
    let id: String?
    let type: CardType
    let tags: [String]
    let deck: String?
    let difficulty: Difficulty?
    let source: String?
    let refs: [String]
}

struct ChoiceOption {
    let isCorrect: Bool
    let content: [CardNode]
}

struct SortItem {
    let correctOrder: Int
    let content: [CardNode]
}

struct MatchPair {
    let left: [CardNode]
    let right: [CardNode]
}
8.3 解析器核心代码结构
class CardDSLParser {

    /// 入口：解析完整文本为多张卡片
    func parseDocument(_ text: String) -> [CardNode] {
        let cardTexts = text.components(separatedBy: "\n===\n")
        return cardTexts.compactMap { parseCard($0) }
    }

    /// 解析单张卡片
    func parseCard(_ text: String) -> CardNode? {
        let meta = parseMeta(text)         // 解析 #type, #tags...
        let sections = splitSections(text)  // 按 ---front--- 等分割
        let front = parseBlocks(sections.front)
        let back = parseBlocks(sections.back)
        let extra = sections.meta.map { parseBlocks($0) }
        return .card(meta: meta, front: front, back: back, extra: extra)
    }

    /// 块级解析
    func parseBlocks(_ text: String) -> [CardNode] {
        // 识别：标题、代码块、选择题组、排序题、匹配题、
        //       判断题、答案线、提示块、分栏、普通段落
    }

    /// 行内解析
    func parseInline(_ text: String) -> [CardNode] {
        // 识别：**bold**, *italic*, `code`, ~~strike~~,
        //       ==highlight==, {{cloze}}, {color:text},
        //       @ref[id]{label}, $LaTeX$, {hint:...}
    }
}

---
九、渲染层设计
9.1 SwiftUI 渲染器
struct CardNodeView: View {
    let node: CardNode

    var body: some View {
        switch node {
        case .text(let str):
            Text(str)

        case .bold(let children):
            HStack(spacing: 0) {
                ForEach(children) { child in
                    CardNodeView(node: child).bold()
                }
            }

        case .colored(let color, let children):
            HStack(spacing: 0) {
                ForEach(children) { child in
                    CardNodeView(node: child)
                        .foregroundColor(Color.fromDSL(color))
                }
            }

        case .cloze(let id, let answer, let hint):
            ClozeView(id: id, answer: answer, hint: hint)

        case .choiceGroup(let options, let multiSelect):
            ChoiceGroupView(options: options, multiSelect: multiSelect)

        case .answerLine(let expected):
            AnswerLineView(expectedAnswer: expected)

        case .cardRef(let id, let label):
            CardRefButton(cardId: id, label: label)

        case .hintBlock(let label, let content):
            DisclosureGroup(label) {
                ForEach(content) { CardNodeView(node: $0) }
            }

        // ... 其他节点类型
        }
    }
}
9.2 交互组件
单选/多选组件
struct ChoiceGroupView: View {
    let options: [ChoiceOption]
    let multiSelect: Bool
    @State private var selected: Set<Int> = []
    @State private var submitted = false

    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            ForEach(options.indices, id: \.self) { i in
                HStack {
                    // 选择指示器
                    Image(systemName: indicatorIcon(index: i))
                        .foregroundColor(indicatorColor(index: i))
                    // 选项内容
                    ForEach(options[i].content) { CardNodeView(node: $0) }
                }
                .onTapGesture { toggle(i) }
            }
            // 提交按钮
            if !submitted {
                Button("提交答案") { submitted = true }
            }
        }
    }
}
填空组件
struct ClozeView: View {
    let answer: String
    let hint: String?
    @State private var userInput = ""
    @State private var revealed = false

    var body: some View {
        if revealed {
            Text(answer)
                .foregroundColor(.green)
                .underline()
        } else {
            TextField(hint ?? "填写答案...", text: $userInput)
                .textFieldStyle(.roundedBorder)
                .frame(width: estimatedWidth)
        }
    }
}

---
十、数据流：从 AI 生成到 App 渲染
10.1 完整流程
用户上传学习材料（文本/PDF/图片）
       │
       ▼
┌──────────────────┐
│  LLM API 调用     │
│  (GPT-4 / Claude) │
│                    │
│  System Prompt:    │
│  Card DSL 格式规范  │
│  +                 │
│  用户学习材料       │
└────────┬─────────┘
         │
         ▼ AI 输出 Card DSL 纯文本
┌──────────────────┐
│  CardDSLParser    │  Swift 端解析器
│  文本 → AST       │
└────────┬─────────┘
         │
         ▼ AST 节点树
┌──────────────────┐
│  CardRepository   │  存入 SwiftData
│  AST → 数据模型    │  （Card.dslContent 保存原文，
│                    │    解析结果缓存）
└────────┬─────────┘
         │
         ▼ SwiftData 查询
┌──────────────────┐
│  CardNodeView     │  递归渲染 AST
│  AST → SwiftUI   │  为 SwiftUI 视图
└──────────────────┘
10.2 数据模型扩展
在之前的技术设计基础上，Card 模型增加 DSL 相关字段：
@Model
final class Card {
    // ... 原有字段 ...

    /// DSL 原文（用于编辑和重新解析）
    var dslContent: String

    /// 卡片类型（从 DSL 解析得出）
    var cardType: CardType  // basic / cloze / choice / multi_choice / fill / composite

    /// 解析后的正面 AST（序列化存储，避免每次重新解析）
    var frontAST: Data?

    /// 解析后的背面 AST
    var backAST: Data?

    /// 关联的卡片 ID 列表（从 @ref 解析）
    var linkedCardIds: [String]
}

---
十一、格式校验与错误处理
11.1 校验规则
规则
说明
错误级别
必须有 ---front---
卡片必须有正面内容
Error
选择题至少一个 (x) / [x]
必须标记正确答案
Error
{{}} 填空不能为空
填空内必须有答案文本
Error
@ref[id] 引用的卡片存在
引用目标不存在时警告
Warning
花括号/方括号配对
标记必须正确闭合
Error
#type 与实际内容匹配
声明 choice 类型但没有选项
Warning
11.2 容错解析
class CardDSLParser {
    var errors: [ParseError] = []
    var warnings: [ParseWarning] = []

    /// 解析失败时降级为纯文本卡片
    func parseCardWithFallback(_ text: String) -> CardNode {
        do {
            return try parseCard(text)
        } catch {
            errors.append(.parseFailed(text, error))
            // 降级：将整段文本作为 basic 卡片的正面
            return .card(
                meta: CardMeta(type: .basic),
                front: [.paragraph([.text(text)])],
                back: [.paragraph([.text("（解析失败，请检查格式）")])],
                extra: nil
            )
        }
    }
}

---
十二、与现有格式的兼容
12.1 导入兼容
来源格式
转换策略
Anki TSV/CSV
front/back 映射为 ---front--- / ---back---，Cloze {{c1::}} 语法直接兼容
Anki .apkg
解析 SQLite 数据库，提取 HTML 字段转为 Card DSL
Markdown 文件
按标题分割，每个 ## 段落生成一张 basic 卡片
纯文本 Q&A
识别 Q: / A: 或 问: / 答: 模式，映射为 front/back
12.2 导出兼容
目标格式
转换策略
Anki TSV
提取 front/back 纯文本，Cloze 转为 Anki {{c1::}} 语法
Markdown
直接输出 Card DSL（本身就是类 Markdown）
PDF
渲染为问答格式的 PDF 文档
JSON
AST 序列化为 JSON，供其他平台使用

---
十三、语法速查表
╔══════════════════════════════════════════════════════╗
║             FlashCard DSL 语法速查表                   ║
╠══════════════════════════════════════════════════════╣
║                                                      ║
║  结构标记                                              ║
║  ─────                                               ║
║  @card / @end        卡片起止                          ║
║  ---front---         正面开始                          ║
║  ---back---          背面开始                          ║
║  ---meta---          元信息开始                         ║
║  ===                 卡片分隔符                         ║
║  #type: basic        题型声明                          ║
║  #tags: a, b         标签                             ║
║  #deck: 牌组名        所属牌组                          ║
║                                                      ║
║  富文本                                                ║
║  ─────                                               ║
║  **加粗**             Bold                             ║
║  *斜体*              Italic                            ║
║  `代码`              Inline code                       ║
║  ==高亮==            Highlight                         ║
║  ~~删除线~~          Strikethrough                      ║
║  {red:红色文字}       Colored text                      ║
║  {bg:yellow:高亮}    Background color                   ║
║  {u:下划线}          Underline                          ║
║  {sup:上标}          Superscript                        ║
║  {sub:下标}          Subscript                          ║
║  {kbd:按键}          Keyboard key                       ║
║  {size:lg:大字}      Font size (sm/md/lg/xl)            ║
║  $LaTeX$             Inline math                       ║
║  $$LaTeX$$           Block math                        ║
║                                                       ║
║  交互元素                                               ║
║  ─────                                                ║
║  {{答案}}             填空                              ║
║  {{答案|提示:xxx}}     带提示填空                         ║
║  {{c1:答案}}          编号填空（生成独立卡片）              ║
║  ( ) 选项 / (x) 选项  单选                              ║
║  [ ] 选项 / [x] 选项  多选                              ║
║  ___(答案)___         答案线                             ║
║  {T} 陈述 / {F} 陈述  判断题                             ║
║  {sort}...{/sort}    排序题                             ║
║  {match}...{/match}  匹配题                             ║
║                                                       ║
║  高级功能                                               ║
║  ─────                                                ║
║  @ref[id]{文字}       卡片引用                           ║
║  !img[描述](url)      图片                              ║
║  !audio[描述](url)    音频                              ║
║  {hint:标题}...{/hint}     折叠提示                      ║
║  {explain:标题}...{/explain} 折叠解析                    ║
║  {columns:N}...{/columns}  分栏布局                     ║
║                                                       ║
╚══════════════════════════════════════════════════════╝