# FlashCard iOS App — 卡片编辑器实现 Prompt

## 你的角色

你是一名资深 iOS 开发工程师，精通 SwiftUI、Swift 5.9+、iOS 17+。你需要实现一个**闪卡制作页面**，风格参考 Notion 的块编辑器（Block Editor），核心要求是**所见即所得（WYSIWYG）**。

---

## ⚠️ 最重要的三条硬性约束（违反即返工）

### 约束 1：所见即所得，禁止"编辑模式/预览模式"切换

- **禁止** 出现任何 "Preview" / "Edit" / "预览" / "编辑" 模式切换按钮或 Tab
- **禁止** 出现 Toggle 在原始文本和渲染结果之间来回切换的交互
- 整个编辑器只有**一种状态**：用户看到的就是最终渲染效果，点击任何内容直接进入原位编辑
- 参考 Notion：你在 Notion 里打字时，**加粗就是加粗的样子，标题就是大字**，不存在切换到"源码模式"去看 `**text**`

### 约束 2：Markdown 实时渲染（输入即渲染）

- 用户输入 `**文字**` 时，输入完第二个 `**` 后，立刻变成 **加粗文字**，原始标记符号消失
- 用户输入 `# 标题` 时，按下空格后，立刻变成大号标题样式
- 用户输入 `` `code` `` 时，闭合后立刻变成 `code` 样式
- 光标移入已渲染的富文本时，**不回退到 Markdown 源码**，而是保持富文本样式并允许原位编辑
- 这和 VS Code 的 Markdown 预览不同——我们不要分屏预览，要**内联渲染**，类似 Typora / Notion

### 约束 3：选择题的编辑态 ≠ 答题态

- **编辑时（制卡阶段）**：选择题的选项旁边显示的是一个"设为正确答案"的开关/勾选框，作者点击它来**标记哪个是正确答案**
- **复习时（答题阶段）**：选项旁边显示的是单选按钮 ○ / 复选框 ☐，用户点击来**作答**
- **绝对禁止**：在编辑器里显示单选按钮让用户"选答案"——这会混淆"制卡"和"答题"两种场景

---

## 一、整体架构：Block Editor（块编辑器）

整个卡片编辑器的核心数据结构是一个 **Block 列表**。每个 Block 是一个独立的内容单元，可以是段落、标题、选择题、填空题等等。

### 1.1 Block 数据模型

```swift
/// 卡片由有序的 Block 列表组成
struct CardDocument {
    var id: UUID = UUID()
    var blocks: [Block]       // 正面内容 blocks
    var backBlocks: [Block]   // 背面内容 blocks（答案/解析）
    var meta: CardMeta
}

/// 单个 Block（内容块）
struct Block: Identifiable {
    let id: UUID
    var type: BlockType
    var content: BlockContent
    var indent: Int = 0       // 缩进层级
}

/// Block 类型枚举
enum BlockType {
    case paragraph          // 普通段落（富文本）
    case heading1           // 一级标题
    case heading2           // 二级标题
    case heading3           // 三级标题
    case bulletList         // 无序列表项
    case numberedList       // 有序列表项
    case codeBlock          // 代码块
    case quote              // 引用块
    case divider            // 分隔线
    case image              // 图片
    case mathBlock          // 数学公式块
    // ---- 闪卡专属题型 Block ----
    case singleChoice       // 单选题
    case multipleChoice     // 多选题
    case fillInBlank        // 填空题
    case answerLine         // 答案线（简答题）
    case trueFalse          // 判断题
    case matching           // 匹配题
    case sorting            // 排序题
    case cloze              // 完形填空
}
```

### 1.2 Block Content 定义

```swift
/// Block 内容（根据 type 使用不同的 content）
enum BlockContent {
    /// 富文本内容（用于 paragraph, heading, bulletList 等）
    case richText(AttributedString)

    /// 选择题内容
    case choice(ChoiceContent)

    /// 填空题内容
    case fillBlank(FillBlankContent)

    /// 答案线内容
    case answerLine(AnswerLineContent)

    /// 判断题内容
    case trueFalse(TrueFalseContent)

    /// 匹配题内容
    case matching(MatchingContent)

    /// 排序题内容
    case sorting(SortingContent)

    /// 完形填空内容
    case cloze(ClozeContent)

    /// 代码块内容
    case code(language: String?, code: String)

    /// 图片内容
    case image(url: String?, alt: String)

    /// 数学公式
    case math(latex: String)

    /// 分隔线（无额外内容）
    case empty
}
```

---

## 二、各题型 Block 的**编辑态**详细设计

### 2.1 单选题 Block（编辑态）

**数据结构：**
```swift
struct ChoiceContent {
    var question: AttributedString   // 题干（富文本）
    var options: [ChoiceOption]
    var isMultiSelect: Bool          // false=单选, true=多选
    var explanation: AttributedString? // 解析（可选）
}

struct ChoiceOption: Identifiable {
    let id: UUID
    var content: AttributedString    // 选项内容（富文本）
    var isCorrect: Bool              // 是否为正确答案
}
```

**编辑态 UI（所见即所得）：**

```
┌─────────────────────────────────────────────────┐
│ 📝 单选题                                    ⋮ ☰ │  ← 左上角：Block 类型标签
│                                                   │     右上角：更多操作 + 拖拽排序手柄
│  以下哪个排序算法的平均时间复杂度是 O(n log n)？      │  ← 题干：富文本编辑区，点击直接编辑
│                                                   │
│  ┌─ A ─────────────────────────────────── ☐ ──┐  │
│  │  冒泡排序                                    │  │  ← ☐ = "设为正确答案"勾选框（未选中）
│  └────────────────────────────────────────────┘  │
│  ┌─ B ─────────────────────────────────── ☑ ──┐  │
│  │  快速排序                                    │  │  ← ☑ = 已标记为正确答案（绿色高亮边框）
│  └────────────────────────────────────────────┘  │
│  ┌─ C ─────────────────────────────────── ☐ ──┐  │
│  │  插入排序                                    │  │
│  └────────────────────────────────────────────┘  │
│  ┌─ D ─────────────────────────────────── ☐ ──┐  │
│  │  选择排序                                    │  │
│  └────────────────────────────────────────────┘  │
│                                                   │
│  ＋ 添加选项                                       │  ← 点击添加新选项
│                                                   │
│  💡 解析（可选）                                    │  ← 折叠区域，点击展开编辑解析
│                                                   │
└─────────────────────────────────────────────────┘
```

**关键交互细节：**

1. **正确答案标记**：每个选项右侧有一个勾选框（☐/☑），作者点击它来标记正确答案。单选模式下只能选一个（点新的自动取消旧的）。**这不是答题！这是在设置答案！**
2. **选项内容编辑**：点击选项文字区域，光标出现，直接编辑文字。支持富文本（加粗、代码等）
3. **选项拖拽排序**：长按选项可以拖拽调整顺序
4. **删除选项**：左滑选项出现删除按钮，或选项右侧更多菜单中有删除
5. **正确选项样式**：被标记为正确的选项有绿色左边框或淡绿背景，视觉上区分

### 2.2 多选题 Block（编辑态）

和单选题结构完全相同，区别：
- `isMultiSelect = true`
- 勾选框允许多个同时选中
- 编辑态 UI 标签显示"📝 多选题"

### 2.3 填空题 Block（编辑态）

**数据结构：**
```swift
struct FillBlankContent {
    var segments: [FillSegment]  // 文本和填空交替
}

enum FillSegment: Identifiable {
    case text(AttributedString)             // 普通文本
    case blank(id: UUID, answer: String, hint: String?)  // 填空位
}
```

**编辑态 UI：**

```
┌─────────────────────────────────────────────────┐
│ 📝 填空题                                    ⋮ ☰ │
│                                                   │
│  TCP 建立连接需要 [ 三次握手    ] ，断开连接需要      │
│  [ 四次挥手    ] 。                                │
│                                                   │
└─────────────────────────────────────────────────┘
```

**关键交互：**

1. **所见即所得**：填空位在编辑时显示为一个带边框的内联输入框，里面显示正确答案
2. **填空位样式**：浅蓝色背景 + 下划线 + 虚线边框，和普通文字有明显区分
3. **插入填空**：用户选中一段文字后，点击工具栏的"填空"按钮，该文字变成填空位（文字内容成为正确答案）
4. **编辑答案**：点击填空位，可以编辑其中的正确答案文字
5. **添加提示**：点击填空位出现浮层，可以输入提示文字
6. **复习时**：填空位显示为空白下划线，用户输入后对比正确答案

### 2.4 答案线 Block（编辑态）

**数据结构：**
```swift
struct AnswerLineContent {
    var prompt: AttributedString?     // 提示文字（如"请写出你的理解："）
    var expectedAnswer: AttributedString? // 参考答案（编辑时设置，复习时不显示）
    var lineCount: Int = 3            // 答案线行数
}
```

**编辑态 UI：**

```
┌─────────────────────────────────────────────────┐
│ 📝 简答题                                    ⋮ ☰ │
│                                                   │
│  请简述 TCP 拥塞控制的四个阶段：                      │  ← 提示文字，点击编辑
│  ─────────────────────────────                    │
│  ─────────────────────────────                    │  ← 答案线（可设置行数）
│  ─────────────────────────────                    │
│                                                   │
│  📌 参考答案（复习时隐藏）                             │  ← 折叠区域
│  慢开始、拥塞避免、快重传、快恢复                        │
│                                                   │
└─────────────────────────────────────────────────┘
```

### 2.5 判断题 Block（编辑态）

**数据结构：**
```swift
struct TrueFalseContent {
    var statement: AttributedString  // 陈述内容
    var isTrue: Bool                 // 正确答案：true/false
    var explanation: AttributedString? // 解析
}
```

**编辑态 UI：**

```
┌─────────────────────────────────────────────────┐
│ 📝 判断题                                    ⋮ ☰ │
│                                                   │
│  TCP 是面向连接的协议。                              │  ← 陈述文字，点击编辑
│                                                   │
│  正确答案：  [✓ 正确]  [ 错误 ]                      │  ← Segmented Control 设置答案
│                                                   │
│  💡 解析：TCP 通过三次握手建立连接...                   │
│                                                   │
└─────────────────────────────────────────────────┘
```

**关键交互：**
- 用 Segmented Control（分段控制器）设置正确答案是"正确"还是"错误"
- 这是在**设置答案**，不是在答题

### 2.6 匹配题 Block（编辑态）

**数据结构：**
```swift
struct MatchingContent {
    var pairs: [MatchPair]
}

struct MatchPair: Identifiable {
    let id: UUID
    var left: AttributedString    // 左侧项
    var right: AttributedString   // 右侧项（配对答案）
}
```

**编辑态 UI：**

```
┌─────────────────────────────────────────────────┐
│ 📝 匹配题                                    ⋮ ☰ │
│                                                   │
│  ┌──────────────┐    ┌──────────────┐            │
│  │ TCP          │ ←→ │ 面向连接      │            │
│  └──────────────┘    └──────────────┘            │
│  ┌──────────────┐    ┌──────────────┐            │
│  │ UDP          │ ←→ │ 无连接        │            │
│  └──────────────┘    └──────────────┘            │
│  ┌──────────────┐    ┌──────────────┐            │
│  │ HTTP         │ ←→ │ 超文本传输     │            │
│  └──────────────┘    └──────────────┘            │
│                                                   │
│  ＋ 添加配对                                        │
│                                                   │
└─────────────────────────────────────────────────┘
```

**关键交互：**
- 编辑时，左右两项在同一行，显示 ←→ 连接符，表示"这两个是一对"
- 点击任一项可编辑文字
- 复习时，右侧打乱顺序，用户拖拽连线

### 2.7 排序题 Block（编辑态）

**数据结构：**
```swift
struct SortingContent {
    var instruction: AttributedString?  // 题目说明
    var items: [SortItem]
}

struct SortItem: Identifiable {
    let id: UUID
    var content: AttributedString
    var correctOrder: Int              // 正确顺序（1, 2, 3...）
}
```

**编辑态 UI：**

```
┌─────────────────────────────────────────────────┐
│ 📝 排序题                                    ⋮ ☰ │
│                                                   │
│  请按平均时间复杂度从低到高排列：                       │
│                                                   │
│  ① ☰ 归并排序 O(n log n)                          │  ← 数字表示正确顺序
│  ② ☰ 希尔排序 O(n^1.3)                            │     ☰ 是拖拽手柄
│  ③ ☰ 冒泡排序 O(n²)                               │     当前排列即正确顺序
│  ④ ☰ 猴子排序 O(n·n!)                             │
│                                                   │
│  ＋ 添加项目                                        │
│                                                   │
└─────────────────────────────────────────────────┘
```

**关键交互：**
- 编辑时，拖拽调整的顺序就是正确顺序
- 复习时，项目被随机打乱，用户拖拽还原正确顺序

### 2.8 完形填空 Block（编辑态）

和填空题类似，但支持编号填空（c1, c2...），同一编号的多个空生成同一张复习卡片。

---

## 三、富文本编辑器（WYSIWYG）实现要求

### 3.1 工具栏

在编辑区域上方或下方显示浮动工具栏：

```
┌─────────────────────────────────────────────────┐
│  B   I   U   S   ~   </>  🔗  📷  Aa▼  🎨▼  f(x) │
│ 加粗 斜体 下划 高亮 删除 代码  链接 图片 字号  颜色 公式 │
└─────────────────────────────────────────────────┘
```

### 3.2 Markdown 快捷输入（实时渲染）

用户可以用 Markdown 语法快捷输入，**输入完成后立刻渲染，标记符号消失**：

| 输入 | 触发时机 | 渲染结果 |
|---|---|---|
| `# 文字` + 空格 | 输入 `# ` 后 | 转为 Heading 1 Block |
| `## 文字` + 空格 | 输入 `## ` 后 | 转为 Heading 2 Block |
| `- ` 或 `* ` | 输入 `- ` 后 | 转为 Bullet List Block |
| `1. ` | 输入 `1. ` 后 | 转为 Numbered List Block |
| `> ` | 输入 `> ` 后 | 转为 Quote Block |
| `---` + 回车 | 输入 `---` 后回车 | 转为 Divider Block |
| ` ``` ` + 回车 | 输入三个反引号后回车 | 转为 Code Block |
| `**文字**` | 输入闭合的 `**` 后 | 文字加粗，`**` 符号消失 |
| `*文字*` | 输入闭合的 `*` 后 | 文字斜体，`*` 符号消失 |
| `` `文字` `` | 输入闭合的 `` ` `` 后 | 文字变为行内代码样式 |
| `==文字==` | 输入闭合的 `==` 后 | 文字高亮，`==` 符号消失 |
| `~~文字~~` | 输入闭合的 `~~` 后 | 文字删除线，`~~` 符号消失 |

**关键：光标移入已渲染的文本时，不回退到 Markdown 源码。始终保持渲染状态，原位编辑。**

### 3.3 Slash Command（`/` 命令）

参考 Notion 的 `/` 命令，用户在空行输入 `/` 弹出菜单：

```
┌─────────────────────────┐
│  / 输入命令...            │
├─────────────────────────┤
│  📝 段落     Paragraph   │
│  𝐇₁ 标题1   Heading 1   │
│  𝐇₂ 标题2   Heading 2   │
│  • 无序列表  Bullet List │
│  1. 有序列表 Number List │
│  ❝ 引用     Quote       │
│  ── 分隔线  Divider     │
│  </> 代码块  Code Block  │
│  ─────────────────────  │
│  🔘 单选题  Single Choice│
│  ☑️ 多选题  Multi Choice │
│  ▁▁ 填空题  Fill Blank   │
│  📝 简答题  Answer Line  │
│  ✓✗ 判断题  True/False   │
│  🔗 匹配题  Matching     │
│  ↕️ 排序题  Sorting      │
│  📎 完形填空 Cloze        │
│  ─────────────────────  │
│  📷 图片    Image        │
│  f(x) 公式  Math Block   │
└─────────────────────────┘
```

---

## 四、颜色系统

### 4.1 文字颜色

支持以下预设颜色，通过工具栏颜色按钮选择：

| 颜色名 | HEX | 用途 |
|---|---|---|
| `default` | 系统默认 | 正常文字 |
| `red` | #E03E3E | 错误标记、重点警告 |
| `orange` | #D9730D | 次要提醒 |
| `yellow` | #DFAB01 | 提示 |
| `green` | #0F7B6C | 正确标记 |
| `blue` | #0B6E99 | 链接、引用 |
| `purple` | #6940A5 | 术语、专有名词 |
| `gray` | #9B9A97 | 辅助说明、注释 |

### 4.2 背景高亮颜色

同上颜色加 `_bg` 后缀，如 `red_bg`、`yellow_bg`，用于文字背景高亮。

---

## 五、Block 操作交互

### 5.1 Block 手柄

每个 Block 左侧有一个 hover 时出现的手柄区域（类似 Notion 的 ⋮⋮ 六点图标）：

```
  ⋮⋮  │ Block 内容...
  │  │
  │  └─ 点击：弹出 Block 操作菜单
  └──── 长按拖拽：调整 Block 顺序
```

**Block 操作菜单：**
```
┌───────────────────┐
│ 🗑 删除            │
│ 📋 复制            │
│ ↕️ 上移 / 下移      │
│ 🔄 转换类型...      │  ← 转换为其他 Block 类型
│ 🎨 颜色 / 背景      │
│ ↩️ 转为正面 / 背面   │  ← 将 Block 移到卡片正面或背面
└───────────────────┘
```

### 5.2 添加新 Block

- **回车**：在当前 Block 后新建一个空白 Paragraph Block
- **`/` 命令**：在空 Block 中输入 `/` 弹出类型选择菜单
- **底部 `+` 按钮**：点击在末尾添加新 Block

### 5.3 删除 Block

- **Backspace**：在空 Block 中按退格键，删除该 Block，光标移到上一个 Block 末尾
- **菜单删除**：通过 Block 手柄菜单删除
- **左滑删除**：iOS 风格左滑出现删除按钮

---

## 六、卡片编辑器页面整体布局

```
┌──────────────────────────────────────────────────┐
│  ← 返回          编辑卡片          保存  ⋯         │  ← 导航栏
├──────────────────────────────────────────────────┤
│                                                    │
│  📂 牌组：英语词汇 ▼    🏷 标签：添加标签...           │  ← 元信息栏
│                                                    │
├──────────────────────────────────────────────────┤
│                                                    │
│  ┌─ 正面（问题）──────────────────────────────┐    │
│  │                                              │    │
│  │  ⋮⋮  ## TCP 三次握手                         │    │  ← Block 1: Heading
│  │                                              │    │
│  │  ⋮⋮  以下关于三次握手的描述，正确的是？         │    │  ← Block 2: Paragraph
│  │                                              │    │
│  │  ⋮⋮  📝 单选题                               │    │  ← Block 3: Choice Block
│  │      A. SYN → SYN+ACK → ACK          ☑     │    │
│  │      B. SYN → ACK → SYN+ACK          ☐     │    │
│  │      C. ACK → SYN → SYN+ACK          ☐     │    │
│  │      ＋ 添加选项                              │    │
│  │                                              │    │
│  │  ⋮⋮  / 输入命令或开始输入...                   │    │  ← 空 Block（占位符）
│  │                                              │    │
│  └──────────────────────────────────────────┘    │
│                                                    │
│  ┌─ 背面（答案）──────────────────────────────┐    │
│  │                                              │    │
│  │  ⋮⋮  **A. SYN → SYN+ACK → ACK**            │    │  ← Block: 答案文字
│  │                                              │    │
│  │  ⋮⋮  > 三次握手确保双方都确认了对方的          │    │  ← Block: 引用块解析
│  │      > 发送和接收能力。                        │    │
│  │                                              │    │
│  │  ⋮⋮  / 输入命令或开始输入...                   │    │  ← 空 Block
│  │                                              │    │
│  └──────────────────────────────────────────┘    │
│                                                    │
├──────────────────────────────────────────────────┤
│  B  I  U  S  </>  🎨  📷  f(x)    ＋ 题型 ▼      │  ← 底部工具栏
└──────────────────────────────────────────────────┘
```

---

## 七、编辑态 vs 复习态对照表

**确保你理解两种状态的区别，不要搞混：**

| 元素 | 编辑态（制卡） | 复习态（答题） |
|---|---|---|
| **单选题选项** | 右侧是 ☐/☑ 勾选框（设置正确答案） | 左侧是 ○/● 单选按钮（作答） |
| **多选题选项** | 右侧是 ☐/☑ 勾选框（设置正确答案） | 左侧是 ☐/☑ 复选框（作答） |
| **填空位** | 显示正确答案文字，蓝色边框可编辑 | 空白下划线，用户输入答案 |
| **答案线** | 下方有"参考答案"编辑区 | 仅显示空白横线，无参考答案 |
| **判断题** | Segmented Control 设置答案 | ✓ / ✗ 两个按钮供用户选择 |
| **匹配题** | 左右成对排列，可编辑配对 | 右侧打乱，用户拖拽连线 |
| **排序题** | 当前顺序就是正确答案，拖拽编辑 | 随机打乱，用户拖拽排序 |
| **富文本** | 可编辑、可格式化 | 只读显示 |
| **卡片正面/背面** | 同时可见，分区域编辑 | 先显示正面，点击翻转显示背面 |

---

## 八、技术实现要点

### 8.1 富文本编辑

- 使用 `AttributedString`（iOS 15+）作为富文本数据模型
- 使用 `UITextView` 包装在 `UIViewRepresentable` 中实现真正的富文本编辑
- **不要用 `TextField` 或 `TextEditor`** — 它们不支持 AttributedString 编辑
- Markdown 快捷输入通过监听 `UITextViewDelegate` 的 `textDidChange` 实现，用正则匹配 Markdown 标记并即时替换为富文本属性

### 8.2 Block 拖拽排序

- 使用 SwiftUI 的 `.draggable()` 和 `.dropDestination()` 实现
- 或使用 `List` + `.onMove()` 实现列表内拖拽

### 8.3 数据持久化

- Block 列表序列化为 JSON 存入 SwiftData
- AttributedString 转 Codable 存储
- 保留一份 Card DSL 纯文本用于 AI 生成/导入导出

### 8.4 性能优化

- 使用 `LazyVStack` 渲染 Block 列表，避免一次性渲染所有 Block
- 富文本渲染结果缓存，避免重复计算
- 图片异步加载 + 缓存

---

## 九、文件结构

```
Views/
├── Editor/
│   ├── CardEditorView.swift           # 编辑器主页面
│   ├── BlockListView.swift            # Block 列表容器
│   ├── BlockView.swift                # 单个 Block 分发渲染
│   ├── Blocks/
│   │   ├── ParagraphBlockView.swift   # 段落 Block（含富文本编辑）
│   │   ├── HeadingBlockView.swift     # 标题 Block
│   │   ├── CodeBlockView.swift        # 代码 Block
│   │   ├── QuoteBlockView.swift       # 引用 Block
│   │   ├── ImageBlockView.swift       # 图片 Block
│   │   ├── DividerBlockView.swift     # 分隔线 Block
│   │   ├── MathBlockView.swift        # 公式 Block
│   │   ├── ChoiceBlockView.swift      # 单选/多选 Block（编辑态）
│   │   ├── FillBlankBlockView.swift   # 填空 Block（编辑态）
│   │   ├── AnswerLineBlockView.swift  # 答案线 Block（编辑态）
│   │   ├── TrueFalseBlockView.swift   # 判断题 Block（编辑态）
│   │   ├── MatchingBlockView.swift    # 匹配题 Block（编辑态）
│   │   └── SortingBlockView.swift     # 排序题 Block（编辑态）
│   ├── Toolbar/
│   │   ├── EditorToolbar.swift        # 底部工具栏
│   │   ├── SlashCommandMenu.swift     # / 命令菜单
│   │   └── ColorPickerPopover.swift   # 颜色选择器
│   └── RichText/
│       ├── RichTextEditor.swift       # 富文本编辑器封装
│       ├── MarkdownShortcuts.swift    # Markdown 快捷输入
│       └── InlineStyler.swift         # 行内样式管理
├── Review/
│   ├── ReviewBlockView.swift          # Block 复习态分发渲染
│   └── ReviewBlocks/
│       ├── ChoiceReviewView.swift     # 选择题（答题态）
│       ├── FillBlankReviewView.swift  # 填空题（答题态）
│       ├── MatchingReviewView.swift   # 匹配题（答题态）
│       └── SortingReviewView.swift    # 排序题（答题态）
```

---

## 十、验收标准 Checklist

请对照以下清单自查，每一项都必须满足：

- [ ] **无编辑/预览模式切换**：整个编辑器只有一种状态，所见即所得
- [ ] **Markdown 实时渲染**：输入 `**文字**` 后自动变加粗，标记符号消失
- [ ] **光标进入已渲染文本不回退源码**：始终保持渲染样式
- [ ] **选择题编辑态用勾选框设置正确答案**：不是单选按钮
- [ ] **选择题复习态用单选/复选按钮作答**：编辑态和复习态明确区分
- [ ] **填空题编辑态显示答案**：蓝色边框内联输入框
- [ ] **填空题复习态隐藏答案**：空白下划线
- [ ] **`/` 命令菜单可用**：输入 `/` 弹出 Block 类型选择
- [ ] **Block 可拖拽排序**：长按手柄拖拽
- [ ] **Block 可删除**：左滑或菜单删除
- [ ] **富文本工具栏可用**：加粗/斜体/颜色/高亮等
- [ ] **正面/背面同时可编辑**：编辑器同时显示正面和背面区域
- [ ] **支持所有题型**：单选/多选/填空/答案线/判断/匹配/排序
